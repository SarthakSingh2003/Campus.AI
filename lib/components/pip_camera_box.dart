import 'package:flutter/material.dart';

// Camera feature removed. Keeping a stub to avoid import breakages if referenced.
class PiPCameraScreen extends StatelessWidget {
  const PiPCameraScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
